let websiteConfig = {
    "requirejs": {
        "paths": {
        }
    },
    "containers": {
        "login": "simple"
    }
}

export type WebsiteConfig = typeof websiteConfig;

export default websiteConfig;